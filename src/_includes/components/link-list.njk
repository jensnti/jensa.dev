{% set getPageLinks = helpers.getPageLinks(content) %}
{% if getPageLinks.length > 0 %}
<h2 id="links">
    <a href="#links"><span class="anchor" aria-hidden="true">#</span></a> Länkar
</h2>
<ul>
    {% for link in getPageLinks %}
    <li>
        <a href="{{ link.url }}">{{ link.title }}</a>
    </li>
    {% endfor %}
</ul>
{% endif %}