{%- from '../nav/macro.njk' import nav -%}
{% set c = "" %}
<header class="site-head">
    <div class="site-head__inner container">
        {{ nav({
        siteName: props.siteName,
        active: props.active,
        items: props.items
        }) }}
        {% set heading = props.title | splitHeading %}
        <h1 class="site-head__title {{ 'site-head__title--extra' if props.active.includes('om') or props.active.includes('projekt') }}">
            {% if heading[0] %}
            <span class="site-head__title--first {{ c | randomColor }}">{{ heading[0] }}</span>
            {% endif %}
            <span class="site-head__title--mid">{{ heading[1] }}</span>
            {% if heading[2] %}
            <span class="site-head__title--last {{ c | randomColor }}">{{ heading[2] }}</span>
            {% endif %}
        </h1>
        {% if props.category or props.tags %}
        <aside class="site-head__meta site-grid">
            {% if props.category %}
            <h2 class="site-head__category">{{ props.category }}</h2>
            {% endif %}
            {% if props.tags %}
            <ul class="site-head__tags">
                {% for tag in props.tags | sort %}
                <li class="tag">
                    <a class="{{ c | randomColor }}" href="/tagg/{{ tag | slugify }}">
                        {{ tag }}
                    </a>
                </li>
                {% endfor %}
            </ul>
            {% endif %}
        </aside>
        {% endif %}
        {% if props.year %}
        <div class="site-head__content">
            <h2 id="{{ props.year }}" class="post-list__title--year">{{ props.year }}</h2>
        </div>
        {% endif %}
    </div>
</header>